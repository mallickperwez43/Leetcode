class Solution {
public:
    bool rotateString(string s, string goal) {
        int m = s.length();
        int n = goal.length();

        if(m != n){
            return false;
        }

        return ((s + s).find(goal) != string :: npos);
    }
};

// TC : O(n)
// SC : O(n)
